{
  "name": "idca-local",
  "version": "1.0.0",
  "description": "IDCA 로컬 매매 시스템 (Firebase-free)",
  "private": true,
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server",
    "build": "npm run build --workspace=server",
    "start": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop ecosystem.config.js",
    "logs": "pm2 logs"
  },
  "engines": {
    "node": ">=20"
  }
}
